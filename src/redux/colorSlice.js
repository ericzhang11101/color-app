import { createSlice } from '@reduxjs/toolkit' 

// slice for all projects

export const colorSlice = createSlice({
    name: 'colors',
    initialState: {

    },
    reducers: {
        createProject: (state) =>{

        },
        removerProject: (state) =>{

        },
        
    }
})


export default colorSlice.reducer